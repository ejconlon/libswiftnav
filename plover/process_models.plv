import prelude;
import constants;

process_model {dim}
  (out F :: Scalar double[dim, dim])
  (out Q :: Diagonal double[dim, dim])
  :: () := (
  F <- scalar 1.0;
  diagonal :: double[dim];
  diagonal[:3] <- (sig_x^2);
  diagonal[3:] <- (sig_z^2);
  Q <- diagonal;
);
